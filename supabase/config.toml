project_id = "sfohvvlqccmmebvjgibx"

[functions.crm-meta-upload-creative]
verify_jwt = true

[functions.crm-meta-upload-creative-v2]
verify_jwt = true

[functions.crm-meta-list-audiences]
verify_jwt = true

[functions.crm-meta-create-lookalike]
verify_jwt = true

[functions.crm-meta-create-website-audience]
verify_jwt = true

[functions.crm-extract-video-dimensions]
verify_jwt = true

[functions.crm-meta-audience-upload]
verify_jwt = true

[functions.fever-ingest-browser]
verify_jwt = false

# Endpoint público chamado pelo portal (mundopropicio.com) com utilizadores
# anónimos: autorização é feita por allowlist de Origin dentro da função.
[functions.crm-google-click-ingest]
verify_jwt = false

# Endpoint público chamado pelo portal para resolver geo a partir do IP:
# autorização por allowlist de Origin dentro da função.
[functions.geo-lookup]
verify_jwt = false

# Só é chamada por triggers/cron da BD com a service_role key do Vault.
[functions.vip-coupon-email]
verify_jwt = true

# Importação de imagens por URL para o portal. Só é chamada com a service_role
# key (via net.http_post/manual); a função compara o Bearer com a service key.
[functions.portal-media-import]
verify_jwt = true

# Chamada pelo destinatário a partir do próprio email (one-click RFC 8058, sem
# JWT) e pela página /unsubscribe do portal. A autorização é o token opaco de
# 32 bytes que vem no URL — não há dados expostos sem ele.
[functions.handle-email-unsubscribe]
verify_jwt = false
# D17 — integração (fecho) de sessões de cartão pré-pago: cria transações pagas.
# Valida JWT e permissões (approve_transactions / raise_budget) dentro da função.
[functions.close-card-session]
verify_jwt = true

# Camada de proposta das faturas de tráfego pago (Meta/Google). Aceita
# service_role ou JWT de utilizador autenticado, validado dentro da função.
[functions.ads-invoice-ingest]
verify_jwt = true

[functions.ads-invoice-apply]
verify_jwt = true

# Auditoria por OCR dos grupos de fatura existentes (admin/platform_admin).
[functions.audit-invoice-groups]
verify_jwt = true

# Sincronização de métricas de audiência via Soundcharts (agregador).
# Aceita service_role ou JWT de admin/platform_admin, validado dentro da função.
[functions.soundcharts-sync]
verify_jwt = true

# Recolha de métricas públicas do Sua Música (perfil + lançamentos).
# Aceita service_role ou JWT de admin/platform_admin, validado dentro da função.
[functions.suamusica-sync]
verify_jwt = true

# Ligação oficial do Instagram dos artistas (Instagram API with Facebook Login).
# start/sync/disconnect exigem JWT; o callback é chamado pelo browser via
# redirect do Facebook (sem JWT) e é autorizado pelo state de uso único.
[functions.artist-meta-oauth-start]
verify_jwt = true

[functions.artist-meta-oauth-callback]
verify_jwt = false

[functions.artist-instagram-sync]
verify_jwt = true

[functions.artist-connection-disconnect]
verify_jwt = true

# Ligação DIRECTA do Instagram (Instagram API with Instagram Login), com app e
# credenciais próprias da carreira artística (INSTAGRAM_APP_ID/SECRET).
# start exige JWT; o callback é chamado pelo browser via redirect do Instagram
# (sem JWT) e é autorizado pelo state de uso único.
[functions.artist-instagram-oauth-start]
verify_jwt = true

[functions.artist-instagram-oauth-callback]
verify_jwt = false

[functions.artist-ads-meta-oauth-start]
verify_jwt = true

[functions.artist-ads-meta-oauth-callback]
verify_jwt = false

[functions.artist-ads-select-account]
verify_jwt = true

[functions.artist-ads-disconnect]
verify_jwt = true

# Geração do plano de tráfego (alvo música) por LLM. Corre SEMPRE com a sessão
# do utilizador: artist_ads_plan_create usa auth.uid() em created_by e valida o
# papel por artist_ads_assert_write. O plano nasce em 'rascunho'; nunca publica.
[functions.artist-ads-strategy-generate]
verify_jwt = true

# Importação de criativos do gestor externo (D-ERP106). Sessão do utilizador
# obrigatória: o papel de tráfego é decidido por artist_ads_assert_write, que
# usa auth.uid() — service_role é recusado.
[functions.artist-ads-creative-import]
verify_jwt = true



# Ligação OFICIAL do TikTok (Display API + Login Kit), app própria
# "Social Music Carreira" (TIKTOK_CLIENT_KEY/SECRET). Mesmo padrão do Instagram:
# start/sync exigem JWT; o callback é chamado pelo browser via redirect do
# TikTok (sem JWT) e é autorizado pelo state de uso único.
[functions.artist-tiktok-oauth-start]
verify_jwt = true

[functions.artist-tiktok-oauth-callback]
verify_jwt = false

[functions.artist-tiktok-sync]
verify_jwt = true

# Renovação dos tokens directos: Instagram (~60 dias) e TikTok (24 h).
[functions.artist-token-refresh]
verify_jwt = true

# Comparáveis por artista (Soundcharts). Só backend; autorização por papel
# (admin/platform_admin/manager/editor) validada dentro de cada função.
[functions.soundcharts-artist-search]
verify_jwt = true

[functions.soundcharts-artist-similar]
verify_jwt = true

[functions.artist-comparable-manage]
verify_jwt = true

# Músicas/obras acompanhadas via Soundcharts (Fase A). Mesmo padrão: só backend,
# papel validado dentro da função (admin/platform_admin/manager/editor).
[functions.soundcharts-song-search]
verify_jwt = true

[functions.artist-song-manage]
verify_jwt = true

[functions.song-soundcharts-sync]
verify_jwt = true

# Benchmark UGC — musicas recentes dos artistas de referencia (comparaveis).
[functions.soundcharts-reference-songs]
verify_jwt = true


# Fase B — vídeos curtos (TikTok/YouTube/Instagram) via Soundcharts.
[functions.artist-shorts-sync]
verify_jwt = true

# Fase C — relatorio de lancamento gerado por LLM.
[functions.artist-song-report]
verify_jwt = true

[functions.process-email-queue]
verify_jwt = true

[functions.partner-statement]
verify_jwt = true

# Anexar um documento a uma ou várias transações sem browser. A origem pode ser
# um URL do Google Drive ou o conteúdo do ficheiro em base64 no próprio pedido
# (conteudo_base64). Só é chamada com a service_role key; a função compara/valida
# o Bearer contra a service key (molde do portal-media-import). Sem JWT de utilizador.
[functions.ingest-transaction-document]
verify_jwt = true

# Ingestão externa de faturas avulsas; autenticação exclusiva por service_role.
[functions.ingest-standalone-invoice]
verify_jwt = true

# Pesquisa autenticada do Manual de Orientação com citações e registo de lacunas.
[functions.help-search]
verify_jwt = true

# Edicao de campanhas Meta ja publicadas (alvo musica). Ver D-ERP99.
[functions.crm-meta-publish-update]
verify_jwt = true

# F5 TikTok (SANDBOX) — motor unico. Host SEMPRE por TIKTOK_API_HOST. Ver D-ERP102.
[functions.crm-tiktok-publish-execute]
verify_jwt = true

[functions.crm-tiktok-publish-activate]
verify_jwt = true

[functions.crm-google-video-publish-execute]
verify_jwt = true

[functions.crm-google-video-publish-activate]
verify_jwt = true

[functions.crm-google-video-metrics-sync]
verify_jwt = false

# Sonda de diagnóstico para o TikTok Artists (artists.tiktok.com).
# Só leitura: não escreve em tabelas nem cria sync_runs/crons.
[functions.tiktok-artists-probe]
verify_jwt = true

# Fase 2 do leitor do TikTok for Artists (D-ERP124). Sonda e sync partilham o
# cookie TIKTOK_ARTISTS_COOKIE; aceita tambem chamada interna por service role.
[functions.tiktok-artists-sync]
verify_jwt = true
